seven <- read.csv("2007.csv", header=TRUE);
ten <- read.csv("2010.csv", header=TRUE);
seven
;
ten;
rownames
names(seven)
gpaSeven <- seven$HS_GPA[seven$Zip == 97303, drop=TRUE];
gpaTen <- ten$HS_GPA[ten$Zip == 97303, drop=TRUE];
gpaSeven;
gpaTen
mean(gpaSeven)
summary(gpaSeven)
summary(gpaTen)
mean(gpaSeven);
gpaTen <- ten$HS_GPA[ten$Zip == 97303, drop=TRUE, exclude=NULL];
gpaTen <- ten$HS_GPA[ten$Zip == 97303, drop=TRUE];
gpaTen
names(seven)
gpaTen <- ten$HS_GPA[ten$Zip == 97303 & ten$Applied = "Y", drop=TRUE];
gpaTen <- ten$HS_GPA[ten$Zip == 97303 & ten$Applied == "Y", drop=TRUE];
gpaTen;
gpaSeven <- seven$HS_GPA[seven$Zip == 97303 & seven$Applied=="Y", drop=TRUE];
gpaSeven;
mean(gpaSeven)
table(gpaSeven);
gpaSeven = gpaSeven[!is.na(a)];
history
history;
history();
history();
gpaSeven
mean(gpaSeven, na.rm=T);
gpaSeven = gpaSeven[!is.na(gpaSeven)];
gpaSeven;
gpaSeven = gpaSeven[!is.null(gpaSeven)];
gpaSeven;
gpaSeven[4] = NA
gpaSeven;
mean(gpaSeven, na.rm=T);
gpaSeven = gpaSeven[!is.na(gpaSeven)];
gpaSeven;
mean(gpaSeven);
gpaSeven = gpaSeven[!is.na(gpaSeven), drop=TRUE];
gpaSeven;
mean(gpaSeven);
mean(c(1, 3, 5));
mine = c(1, 3, 5);
mine;
mean(mine);
gpaSeven;
mean(gpaSeven);
gpaSeven[1];
gpaSeven[2];
gpaSeven;
mean(gpaSeven);
sum(gpaSeven);
add(gpaSeven);
median(gpaSeven);
0 + gpaSeven[1];
gpaSeven = as.numeric(gpaSeven);
gpaSeven;
gpaSeven <- seven$HS_GPA[seven$Zip == 97303 & seven$Applied=="Y", drop=TRUE];
gpaSeven;
  gpaSeven <- seven$HS_GPA[seven$Zip == 97303 & seven$Applied=="Y", drop=TRUE, \
stringsAsFactors=FALSE];
  gpaSeven <- seven$HS_GPA[seven$Zip == 97303 & seven$Applied=="Y", drop=TRUE, stringsAsFactors=FALSE];
  gpaSeven = read.csv("2007.csv", header = TRUE, stringsAsFactors=FALSE);
  gpaSeven <- seven$HS_GPA[seven$Zip == 97303 & seven$Applied=="Y", drop=TRUE];
gpaSeven;
mean(gpaSeven);
0 + gpaSeve[0];
0 + gpaSeven[0];
gpaSeven = as.numeric(as.character(gpaSeven));
mean(gpaSeven);
gpaSeven;
mean(gpaSeven, exclude=NA);
gpaSeven = gpaSeven[!is.na(gpaSeven)];
gpaSeven;
mean(gpaSeven);
source("evalGPA.r");
source("evalGPA.r");
result;
source("evalGPA.r");
evalGPA
evalGPA();
result
result;
result = evalGPA();
result
result = evalGPA();
result
result = null;
result = NA
result = evalGPA();
result;
source("evalGPA.r");
result = evalGPA();
v = c(1, 2, 3);
z = c(4, 5, 6);
v
z
z = c(z, v);
z
names(gpaSeven);
gpaSeven;
names(gpaTen);
    admissionsData = read.csv(i, header = TRUE, stringsAsFactors=FALSE);
    admissionsData = read.csv("2007.csv", header = TRUE, stringsAsFactors=FALSE);
names(admissionsData);
year = admissionsData$EntryYear[1];
newEntry = a[3.55, year];
newEntry = array[3.55, year];
  files <- list.files(pattern=".csv$")
files
length(files);
numFiles <- length(files);
numFiles;
  gpaAveragesByYear <- array(0,ddim=c(numFiles,2);
  gpaAveragesByYear <- array(0,ddim=c(numFiles,2));
  gpaAveragesByYear <- array(0,ddim=c(numFiles,2));
  gpaAveragesByYear ;
lst <- list(GPA = 3.55, year = 2007);
lst
lst = c(lst, (list(GPA = 3.56, year=2008);
lst = c(lst, (list(GPA = 3.56, year=2008)));
lst
lst[1] <- list(GPA = 3.55, year = 2007);
lst <- list(GPA = 3.55, year = 2007);
lst[2] <- list(GPA = 3.44, year = 2008);
lst <- list(GPA = 3.55, year = 2007);
lst = c(lst, (list(GPA = 3.56, year=2008)));
lst
kk[1] =0;
source("evalGPA.r");
source("evalGPA.r");
result = evalGPA();
source("evalGPA.r");
result = evalGPA();
result
source("evalGPA.r");
result
result = evalGPA();
result
source("evalGPA.r");
result = evalGPA();
result
source("evalGPA.r");
source("evalGPA.r");
result = evalGPA();
result
source("evalGPA.r");
result = evalGPA();
pwd
result = evalGPA();
exit
quit()
